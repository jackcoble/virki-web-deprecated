export enum PAGES {
    ROOT = "/",
    REGISTER = "/signup",
    VERIFY = "/verify",
    CREDENTIALS = "/credentials",
    VAULT = "/vaults",
    EDIT_VAULT = "/vaults/edit",
    NEW_VAULT = "/vaults/new",
    PROFILE = "/profile"
}