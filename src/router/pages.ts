export enum PAGES {
    ROOT = "/",
    REGISTER = "/signup",
    VERIFY = "/verify",
    CREDENTIALS = "/credentials",
    VAULT = "/vaults",
    NEW_VAULT = "/vaults/new",
    PROFILE = "/profile"
}