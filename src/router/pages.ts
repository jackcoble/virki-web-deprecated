export enum PAGES {
    LOGIN = "/login",
    REGISTER = "/signup",
    LOCK = "/lock",
    VAULT = "/",
    FAVOURITES = "/favourites",
    EDIT_VAULT = "/vaults/edit",
    NEW_VAULT = "/vaults/new",

    // Account management
    ACCOUNT = "/account",
    ACCOUNT_SESSIONS = "/account/sessions"
}