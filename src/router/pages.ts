export enum PAGES {
    LOGIN = "/login",
    REGISTER = "/signup",
    LOCK = "/lock",
    VAULT = "/vault",
    FAVOURITES = "/favourites",

    // Account management
    ACCOUNT = "/account",
    ACCOUNT_SESSIONS = "/account/sessions"
}